# Soroban Guardrail Client

Open-source static analysis and deployment readiness checks for Soroban smart contracts.

## Run

```powershell
npm install
npm run build
npm start
```

The preview server listens on `http://localhost:5173`.
